import React from 'react';
import { Container } from 'react-bootstrap';

const Header = () => {
  return (
    <section className="hero">
      <Container>
        <div className="hero__content"> test</div>
      </Container>
    </section>
  );
};

export default Header;
